{
  "name": "koki-adventure-note",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute koki-adventure-db --file=schema.sql",
    "db:init:remote": "wrangler d1 execute koki-adventure-db --remote --file=schema.sql"
  }
}
