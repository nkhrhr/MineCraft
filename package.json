{
  "name": "minecraft-story-addon",
  "version": "1.0.0",
  "description": "Koki's Minecraft Bedrock story addon",
  "scripts": {
    "validate": "node scripts/validate.js",
    "check-uuids": "node scripts/check-uuids.js",
    "pack": "node scripts/pack.js",
    "create-world": "node scripts/create-world.js",
    "bump-version": "node scripts/bump-version.js",
    "release": "npm run validate && npm run pack && npm run create-world",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "archiver": "^7.0.0"
  }
}
